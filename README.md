```markdown
# 💜 FibroSite - Conscientização sobre Fibromialgia

Este é o repositório do projeto **FibroSite**, um website informativo e empático desenvolvido para oferecer conteúdo claro e acessível sobre a **Fibromialgia**. O objetivo é aumentar a conscientização, reduzir o estigma e fornecer informações sobre sintomas, diagnóstico e tratamento.

## 🚀 Tecnologias Utilizadas

O projeto foi construído utilizando o framework Angular e ferramentas modernas de desenvolvimento:

* **Framework:** Angular 21.0.0
* **Linguagem:** TypeScript
* **Estilização:** SCSS (Sass) e CSS
* **Componentes UI:** Angular Material (v21.0.1)
* **Animações de Texto:** Typed.js

## 🛠️ Configuração e Instalação

Para configurar o projeto em sua máquina local, siga os passos abaixo.

### Pré-requisitos

Certifique-se de ter o **Node.js** e o **npm** (ou Yarn/pnpm) instalados em sua máquina. O Angular CLI é necessário para rodar o projeto.

1.  **Instale o Angular CLI** globalmente:
    ```bash
    npm install -g @angular/cli
    ```

### Instalação das Dependências

Na pasta raiz do projeto (`fibrosite`), instale as dependências:

```bash
npm install
